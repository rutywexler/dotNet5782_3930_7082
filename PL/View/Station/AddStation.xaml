<Window x:Class="PL.View.Station.AddStation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.ViewModel.Station"
         mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="AddStation" Height="450" Width="800" >
    <Window.DataContext>
        <local:AddStationViewModel/>
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding BaseStation.Id, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Id" Margin="5"/>
            <TextBox Text="{Binding BaseStation.Name, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Name" Margin="5"/>
            <TextBox Text="{Binding BaseStation.ChargeSlots, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Empty charge slots" Margin="5"/>
            <TextBox Text="{Binding BaseStation.Location.Latitude, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Latitude" Margin="5"/>
            <TextBox Text="{Binding BaseStation.Location.Longitude,  UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Longitude" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
