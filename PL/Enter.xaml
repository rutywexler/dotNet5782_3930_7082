<Window x:Class="PL.Enter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Enter" Height="550" Width="1000">

    <Grid>
        <Grid x:Name="aside" Margin="605,242,-20,0" >
            <Grid.Background>
                <ImageBrush ImageSource="./Images/background2png-01.png"/>
            </Grid.Background>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
        </Grid>

        <TextBlock Text="Enter code here:"  FontSize="50" Margin="2,86,-2,-86" />
        <TextBox x:Name="Password"  Width="300" Height="100"  FontSize="80" Text="{Binding CODE}" VerticalAlignment="Center" HorizontalAlignment="Left" />
        <Button Content="OK" Width="100" Click="Button_Click" VerticalAlignment="Center"/>
    </Grid>
</Window>
