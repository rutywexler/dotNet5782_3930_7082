<Window x:Class="PL.Customers.CustomerListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Customers"
        mc:Ignorable="d"
        Title="CustomerListView" Height="450" Width="800">
    <Grid>
        <ListView Grid.Row="1" Margin="0,5,0,-5" x:Name="DronesListView" ItemsSource="{Binding}"  >
            <!--MouseDoubleClick="ViewDrone"-->>
            <ListView.ItemTemplate >
                <DataTemplate>
                    <Border BorderBrush="Red">
                        <StackPanel >
                            <TextBlock Text="{Binding }"></TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Content="Add new customer" HorizontalAlignment="Left" Margin="580,268,0,0" Grid.Row="1" VerticalAlignment="Top" Height="90" Width="158" Click="OpenAddCustomr"/>
        <!--<Button Content="Cancel filtering" HorizontalAlignment="Left" Margin="600,42,0,0" Grid.Row="1" VerticalAlignment="Top" Height="60" Width="120" Click="Cancel_filtering"/>-->

    </Grid>
</Window>
